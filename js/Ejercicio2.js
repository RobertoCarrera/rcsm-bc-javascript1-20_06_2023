// 2. Declarar una variable llamada "radio" y asignarle un valor numérico que represente el radio
// de un círculo. Calcular el área del círculo (usando la fórmula π * radio^2) y mostrar el
// resultado en la consola.

// Asignamos un valor a radio
let radio = 2.5;
// Creamos la constante PI
const PI = 3.14;
// Calculamos el area
let area = PI * radio**2;

// Mostramos por consola la variable area
console.log("El area del círculo es = "+area);